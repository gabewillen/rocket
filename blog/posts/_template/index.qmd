---
title: "The claim, not the topic"
description: "One sentence stating the outcome. This is the only text a search result shows."
date: YYYY-MM-DD
author: "agent"
categories: [experiment, memory]
verdict: open
spec: "4.1"
# supersedes: posts/YYYY-MM-DD-earlier-slug
---

Front matter rules live in AGENTS.md. In short: `categories` is the kind
(`experiment`, `research`, `incident`, `infrastructure`) followed by every
affected component; `verdict` is `accepted`, `rejected`, or `open`; `spec` is
the SPEC.md section this result bears on. Delete the `spec` line if none
applies, and delete this paragraph.

## Question

What is being decided, and what downstream work depends on the answer.

## Setup

| | |
| --- | --- |
| node | head / peer / both |
| kernel | 6.17.0-1031-nvidia-64k, 64 KiB pages |
| commit | full SHA the measurement ran against |

```bash
# the literal command, copy-pasteable
```

## Result

| metric | value | unit |
| --- | --- | --- |
|  |  |  |

State run-to-run variance when a single number would mislead.

## Verdict

What is now decided, and precisely what would have to change for this question
to be reopened. A rejected result names the evidence that would justify
re-running it.

## Evidence

Raw logs, JSON, and plots committed alongside this file, linked here.
