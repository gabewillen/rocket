---
title: "Design"
subtitle: "The compiler, the accelerant it emits, and the rules both follow."
date-modified: 2026-09-05
toc-depth: 2
---

Current state, revised in place. The [log](index.qmd) is the append-only record
of how each decision was reached, and holds every measurement cited here.

A wildfire is what happens when a spark ignites an accelerant.

| term | meaning |
| --- | --- |
| accelerant | an inference engine compiled for one model on one cluster. Regenerated, not reconfigured. |
| spark | the hardware that ignites it (DGX Spark, GB10) |
| wildfire | the result of the spark igniting the accelerant |

Model-specific content lives with its [accelerant](accelerants/index.qmd).
[GLM-5.3-Flash](accelerants/glm-5.3-flash.qmd) is the first. Hardware
measurements live in the log.

## Doctrine {#doctrine}

General-purpose engines pay for generality at runtime: dynamic shapes, runtime
dtype dispatch, autotuned kernel selection, a general allocator, and a Python
step loop on a 20-core Arm host. They reach
[132.8 tok/s at c32](posts/baseline/2026-09-05-general-engine-baselines/) here.
An accelerant recovers that cost by refusing to be general.

One model. One cluster topology. One weight format. Every shape known ahead of
time. That binds each accelerant, not the compiler. A second model means a
second accelerant, never a branch in this one.

## Structure {#structure}

The compiler runs offline on the target hardware. It consumes the model
checkpoint, a hardware profile, and an expert co-activation profile gathered
from a representative corpus. It emits a plan: a pre-swizzled weight blob, a
static memory map with literal byte offsets, an expert-to-node placement, a
kernel selection with pinned tile parameters, the execution graph topology, and
the cache arena geometry.

The accelerant maps that plan and executes it. No dynamic allocator, no
autotuner, no dtype or shape dispatch, no kernel fallback paths, no Python in
the step loop. Startup either maps the plan or refuses to start.

Changing the model is a compiler run producing a new accelerant.

## What specialization buys {#buys}

| | |
| --- | --- |
| memory | residency is decided offline down to byte offsets and validated once. No utilization fraction to tune, nothing discovered by allocating until failure. |
| launch overhead | fixed shapes make the decode step one graph launch per batch bucket. Buckets are a compiled closed set (1, 2, 4, 8, 16, 32) with no shape wildcard. |
| weight layout | experts are pre-swizzled offline into the layout the GEMM wants, 64 KiB aligned, so a top-k gather is a few aligned contiguous reads. No runtime transposition or padding discovered at load time. |
| expert placement | the compiler places experts across nodes from measured co-activation. A general engine cannot, because it learns the model at load time. |
| TLB coverage | the weight arena is one hugepage-backed mapping, 512 MiB pages over a per-node working set in the tens of GiB. |

## What we give up {#gives-up}

Arbitrary models, runtime-selectable dtypes, plugin backends, dynamic adapters,
arbitrary parallelism degrees, and degradation onto unsupported hardware. These
are out of scope rather than pending. A proposal to add one has to argue against
the throughput it costs.

A reference implementation (HF/PyTorch) is kept as an offline parity oracle and
for golden-vector regression. It never appears in the serving path.

## Rules an accelerant must satisfy {#rules}

Each rule follows from a measurement in the log.

### Memory budget {#memory}

Take an absolute byte budget. Never a fraction of `MemTotal`
([why](posts/hardware/2026-09-05-memtotal-is-not-capacity/)).

```
usable = measured_cuda_free_after_registration
       - registration_reserve      (probed at startup)
       - os_floor                  (hard minimum 5 GiB)
```

Refuse to start if requested residency exceeds `usable`. `MemTotal` may appear
in logs, never in an arithmetic path that sizes an allocation.

### Alignment {#alignment}

64 KiB is the universal quantum: O_DIRECT I/O, mmap offsets, cache block rows,
copy descriptors, shared-region layout. Constants inherited from 4K-page code
are wrong until re-derived.

### Host registration {#registration}

Serialize ranks with a file lock, register in bounded row-aligned chunks, roll
back to unpinned on failure, unregister on every exit path, and never register a
regular-file mapping
([protocol](posts/memory/2026-09-05-host-registration-protocol/)).

### Cluster {#cluster}

Page size is a compiler input baked into the plan's alignment constants. A node
whose page size disagrees is refused at handshake. Both rails are required;
single-rail is a startup refusal, because the compiled all-to-all schedule
assumes two.

### Kernels {#kernels}

Accelerants own their attention and expert kernels. Every kernel declares its
dynamic shared-memory requirement and is rejected at build time above the GB10
ceiling ([limits](posts/hardware/2026-09-05-gb10-execution-limits/)).

### Reusable state {#state}

Whatever a layer needs to resume a sequence is one cache entry, written and
restored atomically at a shared token boundary. An attention KV cache alone is
not sufficient for models with recurrent state.

### Numerics {#numerics}

Precision is fixed at compile time per layer. No runtime dtype selection, no
mixed-precision fallback. A quality gate runs on every numerics change.

## Observability {#telemetry}

Telemetry goes through OpenTelemetry. Required signals: per-step time split
(attention, expert, all-to-all, sampling), bytes read per token, cache hit
rates, prefix restore latency, registration reserve against measured CUDA free,
and per-rail fabric utilization.

Cardinality is bounded by construction. Request IDs, prompt hashes, and expert
IDs are never metric dimensions. Per-request detail lives on spans.

## API {#api}

OpenAI-compatible `/v1/chat/completions` and `/v1/completions` with streaming,
tool calling, and reasoning-content separation. Image input in scope, video out
of scope.

Per-request knobs that would force a shape or kernel choice the plan did not
compile are rejected. Serving-level controls are plan parameters, set at compile
time and reported read-only.
